[Unit]
Description=Photo API
After=network.target

[Service]
User=pii
WorkingDirectory=/home/pii/photo_api
ExecStart=/home/pii/.local/bin/uvicorn server:app --host 0.0.0.0 --port 8000

Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
